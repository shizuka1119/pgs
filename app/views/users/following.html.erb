<div class="main user-show">
  <div class="container">
    <div class="user">
      <img src="<%= "/user_images/#{@user.profile_image}" %>">
      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>
      <% if @user.id == @current_user.id %>
        <%= link_to "Edit", edit_user_path %>
      <% end %>
    </div>
    <ul class="user-tabs">
      <li><%= link_to("Note", "/users/#{@user.id}") %></li>
      <li><%= link_to("Good", "/users/#{@user.id}/likes") %></li>
      <li class="active"><%= link_to("Following", "/users/#{@user.id}/following") %></li>
      <li><%= link_to("Follower", "/users/#{@user.id}/follower") %></li>
    </ul>
    <% @relationships.each do |r| %>
      <% user = User.find_by(id: r.user_id) %>
      <div class="items-index-item">
        <div class="item-left">
          <img src="<%= "/user_images/#{r.user.profile_image}" %>">
        </div>
        <div class="item-right">
          <div class="item-user-name">
            <%= link_to(r.follow.name, "/users/#{r.follow.id}") %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

