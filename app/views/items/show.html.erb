<h1>記事詳細</h1>

<table class="table">
		<thead class="thead-light">
			<tr class="active">
				<th>記事ID</th>
				<th>タイトル</th>
				<th>ジャンル</th>
				<th>内容</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td><%= @item.id %></td>
				<td><%= @item.title %></td>
				<td><%= @item.category %></td>
				<td><%= @item.body %></td>
			</tr>
		</tbody>
	</table>

<%= link_to "Edit", edit_item_path %>
<%= link_to "index", items_path %>
<%= link_to "Destory", destroy_item_path %>