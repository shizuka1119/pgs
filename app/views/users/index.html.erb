<h1>ユーザー一覧</h1>

<table class="table">
		<thead class="thead-light">
			<tr class="active">
				<th>名前</th>
				<th>投稿</th>
		</thead>
		<% @users.each do |user| %>
		<tbody>
			<tr>
				<td><%= link_to "show", user.name %></td>
			</tr>
		</tbody>
		<% end %>
</table>